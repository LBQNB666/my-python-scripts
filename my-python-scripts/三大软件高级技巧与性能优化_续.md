def optimize_images_for_ppt(image_paths):
    """优化图片用于 PPT"""
    optimized = []
    
    for path in image_paths:
        with Image.open(path) as img:
            # 转换为 RGB
            if img.mode in ('RGBA', 'P'):
                img = img.convert('RGB')
            
            # 调整大小（PPT 标准分辨率 96 DPI）
            max_size = (1920, 1080)  # Full HD
            img.thumbnail(max_size, Image.LANCZOS)
            
            # 保存到内存
            buffer = io.BytesIO()
            img.save(buffer, format='JPEG', quality=85)
            buffer.seek(0)
            
            optimized.append(buffer)
    
    return optimized

# 批量添加图片的优化版本
def batch_add_images_optimized(slide, image_paths, start_left, start_top, spacing):
    """批量添加图片（优化版）"""
    
    left = start_left
    top = start_top
    
    for path in image_paths:
        # 使用优化后的图片
        optimized_img = optimize_single_image(path)
        
        # 添加到幻灯片
        from pptx.util import Inches
        slide.shapes.add_picture(
            optimized_img,
            Inches(left), Inches(top),
            width=Inches(2)  # 统一宽度
        )
        
        # 更新位置
        left += spacing
        if left > 8:  # 换行
            left = start_left
            top += 2
```

---

## 🎯 高级应用场景

### 1. 跨软件联动

```python
# Word + Excel + PPT 联动

def create_full_report(excel_data, word_template, output_pptx):
    """从 Excel 数据生成 Word 报告，再生成 PPT"""
    
    # 1. 从 Excel 读取数据
    import pandas as pd
    df = pd.read_excel(excel_data)
    
    # 2. 生成 Word 报告
    from docx import Document
    doc = Document()
    doc.add_heading('数据分析报告', 0)
    
    # 添加表格
    table = doc.add_table(rows=len(df)+1, cols=len(df.columns))
    for i, col in enumerate(df.columns):
        table.rows[0].cells[i].text = col
    for row_idx, row in df.iterrows():
        for col_idx, value in enumerate(row):
            table.rows[row_idx+1].cells[col_idx].text = str(value)
    
    doc.save('report.docx')
    
    # 3. 从 Word 报告提取关键数据生成 PPT
    from pptx import Presentation
    
    prs = Presentation()
    slide = prs.slides.add_slide(prs.slide_layouts[1])
    slide.shapes.title.text = '数据摘要'
    
    # 提取关键指标
    summary = f'总数据量: {len(df)} 条\n'
    summary += f'列数: {len(df.columns)}\n'
    summary += f'数值列平均: {df.select_dtypes(include="number").mean().mean():.2f}'
    
    slide.placeholders[1].text = summary
    
    prs.save(output_pptx)
```

### 2. 模板引擎

```python
# 简单的模板引擎

class OfficeTemplate:
    """Office 文档模板引擎"""
    
    def __init__(self, template_path):
        self.template_path = template_path
        self.variables = {}
    
    def set(self, key, value):
        """设置变量"""
        self.variables[key] = value
    
    def render_word(self, output_path):
        """渲染 Word 文档"""
        from docx import Document
        
        doc = Document(self.template_path)
        
        for key, value in self.variables.items():
            placeholder = f'{{{{{key}}}}}'
            
            # 替换段落中的占位符
            for para in doc.paragraphs:
                if placeholder in para.text:
                    for run in para.runs:
                        if placeholder in run.text:
                            run.text = run.text.replace(placeholder, str(value))
            
            # 替换表格中的占位符
            for table in doc.tables:
                for row in table.rows:
                    for cell in row.cells:
                        if placeholder in cell.text:
                            for para in cell.paragraphs:
                                for run in para.runs:
                                    if placeholder in run.text:
                                        run.text = run.text.replace(placeholder, str(value))
        
        doc.save(output_path)
    
    def render_excel(self, output_path):
        """渲染 Excel 文档"""
        from openpyxl import load_workbook
        
        wb = load_workbook(self.template_path)
        
        for ws in wb.worksheets:
            for row in ws.iter_rows():
                for cell in row:
                    if cell.value and isinstance(cell.value, str):
                        for key, value in self.variables.items():
                            placeholder = f'{{{{{key}}}}}'
                            if placeholder in cell.value:
                                cell.value = cell.value.replace(placeholder, str(value))
        
        wb.save(output_path)
    
    def render_ppt(self, output_path):
        """渲染 PPT 文档"""
        from pptx import Presentation
        
        prs = Presentation(self.template_path)
        
        for key, value in self.variables.items():
            placeholder = f'{{{{{key}}}}}'
            
            for slide in prs.slides:
                for shape in slide.shapes:
                    if hasattr(shape, 'text_frame'):
                        for para in shape.text_frame.paragraphs:
                            for run in para.runs:
                                if placeholder in run.text:
                                    run.text = run.text.replace(placeholder, str(value))
        
        prs.save(output_path)

# 使用
template = OfficeTemplate('template.docx')
template.set('name', '张三')
template.set('date', '2026-04-21')
template.set('company', '某某公司')
template.render_word('filled.docx')
```

---

## 📊 性能对比

| 操作 | 普通方法 | 优化方法 | 提升 |
|------|----------|----------|------|
| **Excel 写入 100万行** | ~60秒, 3GB内存 | ~20秒, 500MB | **3x** |
| **Excel 读取 100万行** | ~30秒 | ~10秒 (read_only) | **3x** |
| **PPT 批量生成 100个** | ~50秒 | ~20秒 (并行) | **2.5x** |
| **Word 批量插入图片** | ~30秒 | ~15秒 (预处理) | **2x** |

---

## 🔧 调试技巧

```python
# 调试 Office 文件问题

def debug_docx(file_path):
    """调试 Word 文档"""
    from docx import Document
    import zipfile
    from lxml import etree
    
    # 检查是否可以打开
    try:
        doc = Document(file_path)
        print('✓ 文档可以打开')
    except Exception as e:
        print(f'✗ 打开失败: {e}')
        return
    
    # 检查 XML 结构
    with zipfile.ZipFile(file_path, 'r') as zf:
        xml_content = zf.read('word/document.xml')
        root = etree.fromstring(xml_content)
        print(f'✓ XML 结构正常，元素数量: {len(root)}')

def debug_xlsx(file_path):
    """调试 Excel 文档"""
    from openpyxl import load_workbook
    
    # 检查是否可以打开
    try:
        wb = load_workbook(file_path)
        print(f'✓ 可以打开，共 {len(wb.sheetnames)} 个工作表')
        
        for sheet_name in wb.sheetnames:
            ws = wb[sheet_name]
            print(f'  - {sheet_name}: {ws.max_row}行 x {ws.max_column}列')
        
        wb.close()
    except Exception as e:
        print(f'✗ 打开失败: {e}')

def debug_pptx(file_path):
    """调试 PPT 文档"""
    from pptx import Presentation
    
    try:
        prs = Presentation(file_path)
        print(f'✓ 可以打开，共 {len(prs.slides)} 张幻灯片')
        
        for i, slide in enumerate(prs.slides, 1):
            print(f'  幻灯片 {i}: {len(slide.shapes)} 个形状')
    except Exception as e:
        print(f'✗ 打开失败: {e}')
```

---

## 📋 最佳实践清单

### Word
- [ ] 使用 `write()` 而不是多次 `add_paragraph()`
- [ ] 批量操作时禁用样式自动更新
- [ ] 处理完后调用 `gc.collect()`
- [ ] 中文设置 `w:eastAsia` 字体
- [ ] 表格设置 `<w:cantSplit/>` 防止跨页断开

### Excel
- [ ] 大文件使用 `write_only=True`
- [ ] 大文件读取使用 `read_only=True`
- [ ] 记得 `wb.close()`
- [ ] 公式需要 Excel 打开才会计算
- [ ] 日期设置 `number_format`

### PPT
- [ ] 批量生成时定期 `gc.collect()`
- [ ] 图片预处理压缩
- [ ] 字体设置 `eastAsia`
- [ ] 复用布局